name: GraphCombinations matrix-free transform research

on:
  pull_request:
    branches:
      - research-gc-amortized-loop-workspace
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
    paths:
      - '.github/workflows/GraphCombinationsMatrixfreeTransform.yml'
      - 'research/graphcombinations_matrixfree_loop_transform.jl'
      - 'research/graphcombinations_loop_corpus_certification.jl'
      - 'src/**'
      - 'Project.toml'
  workflow_dispatch:

jobs:
  residual:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: '1'
      - uses: julia-actions/cache@v3
      - name: Route KC loop adapter through packed candidate
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("src/collision_momentum_graphcombinations.jl")
          text = path.read_text()
          replacements = (
              (
                  "workspace::GC.DirectedCanonicalizationWorkspace\n        buffer::GC.DirectedCanonicalizationBuffer\n        graph::GC.DirectedGCGraphBuffer",
                  "workspace::GC.PackedDirectedCanonicalizationWorkspace\n        buffer::GC.DirectedCanonicalizationBuffer\n        graph::GC.DirectedGCGraphBuffer",
              ),
              (
                  "GC.DirectedCanonicalizationWorkspace(capacity),",
                  "GC.PackedDirectedCanonicalizationWorkspace(capacity),",
              ),
              (
                  "GC.canonicalize_directed!(\n            storage.buffer, storage.workspace, storage.graph, storage.labels\n        )",
                  "GC.canonicalize_directed_packed!(\n            storage.buffer, storage.workspace, storage.graph, storage.labels\n        )",
              ),
          )
          for old, new in replacements:
              if text.count(old) < 1:
                  raise SystemExit(f"packed adapter patch target not found: {old!r}")
              text = text.replace(old, new, 1)
          path.write_text(text)
          PY
      - name: Use structural quotient semantic oracle
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("research/graphcombinations_matrixfree_loop_transform.jl")
          text = path.read_text()
          old = """        @test matrixfree_gc_quotient(expression, workspace) ==
            prepared_dense_gc_quotient(expression, workspace)
"""
          new = """        @test KC.loop_quotient_terms(matrixfree_gc_quotient(expression, workspace)) ==
            KC.loop_quotient_terms(prepared_dense_gc_quotient(expression, workspace))
"""
          if text.count(old) != 1:
              raise SystemExit("matrix-free semantic oracle patch target not found")
          path.write_text(text.replace(old, new, 1))
          PY
      - name: Instantiate baseline environment
        run: julia --project=. -e 'using Pkg; Pkg.instantiate()'
      - name: Pin exact GraphCombinations packed candidate
        run: julia --project=. -e 'using Pkg; Pkg.add(url="https://github.com/oameye/GraphCombinations.jl", rev="f8ff5edf0f7b1cec29a4046d19b1d48e597c2859")'
      - name: Add benchmark harness
        run: julia --project=. -e 'using Pkg; Pkg.add("BenchmarkTools")'
      - name: Extract two-loop corpus definitions
        shell: bash
        run: |
          sed 's/const CORPUS_LOOP_COUNTS = 1:5/const CORPUS_LOOP_COUNTS = 2:2/' \
            research/graphcombinations_loop_corpus_certification.jl | \
            awk '/^cases = corpus_cases\(\)/ { exit } { print }' > /tmp/kc_corpus_defs.jl
      - name: Certify matrix-free residual path
        shell: bash
        env:
          KC_CORPUS_DEFS: /tmp/kc_corpus_defs.jl
        run: |
          set -o pipefail
          julia --project=. research/graphcombinations_matrixfree_loop_transform.jl \
            2>&1 | tee graphcombinations-matrixfree-transform.log
      - name: Preserve diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: graphcombinations-matrixfree-transform
          path: graphcombinations-matrixfree-transform.log
